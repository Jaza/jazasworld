---
layout: layouts/base.njk
---
<div class="content">

{{ writingsBlurb | safe }}

{{ recentWritingTitle | safe }}

{%- set writingItems = collections.writings | reverse %}
{%- if writingItems.length %}
  {%- set writingItem = writingItems | first %}
{% include "components/writing_teaser.njk" %}
{%- else %}
<p><em>No writings found.</em></p>
{%- endif %}

{{ bestWritingTitle | safe }}

{%- set writingItems = collections.bestwriting %}
{%- if writingItems.length %}
  {%- set writingItem = writingItems | first %}
{% include "components/writing_teaser.njk" %}
{%- else %}
<p><em>No writings found.</em></p>
{%- endif %}

{{ booksBlurb | safe }}

{{ recentBookTitle | safe }}

{%- set reviewItems = collections.review | reverse %}
{%- if reviewItems.length %}
  {%- set reviewItem = reviewItems | first %}
{% include "components/review_teaser.njk" %}
{%- else %}
<p><em>No books found.</em></p>
{%- endif %}

{{ bestBookTitle | safe }}

{%- set reviewItems = collections.bestreview %}
{%- if reviewItems.length %}
  {%- set reviewItem = reviewItems | first %}
{% include "components/review_teaser.njk" %}
{%- else %}
<p><em>No books found.</em></p>
{%- endif %}

{{ booksNote | safe }}

</div><!-- /.content -->
